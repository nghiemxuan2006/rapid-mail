server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: route everything to index.html so React Router (BrowserRouter)
    # can handle client-side routes like /campaigns on direct access / refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
